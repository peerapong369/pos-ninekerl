# POS NineKerl

ระบบ Point of Sale สำหรับร้านก๋วยเตี๋ยวที่ออกแบบให้ใช้งานผ่านเว็บเบราว์เซอร์ ลูกค้าสแกน QR Code ของแต่ละโต๊ะเพื่อเลือกเมนูและส่งออเดอร์ได้ทันที ครัวและผู้ดูแลร้านติดตามสถานะ การชำระเงิน และสต๊อกวัตถุดิบได้จากแดชบอร์ดเดียว

## ไฮไลท์ฟีเจอร์
- 📱 **สั่งอาหารผ่าน QR Code รายโต๊ะ** – ลูกค้าดูเมนู, ใส่หมายเหตุ และติดตามสถานะออเดอร์ของโต๊ะตัวเองได้
- 🍳 **หน้าครัวอัปเดตอัตโนมัติ + เสียงแจ้งเตือน** – ครัวเห็นออเดอร์ใหม่ทันที เปลี่ยนสถานะได้ และเชื่อมไปหน้าเช็คบิล
- 💵 **ระบบคิดเงิน/ใบเสร็จ** – รองรับส่วนลด ค่าบริการ ภาษี บันทึกการชำระหลายช่องทาง และพิมพ์ใบเสร็จได้
- 📦 **บริหารสต๊อกวัตถุดิบ** – แยกวัตถุดิบ, สูตรต่อเมนู, บันทึกการใช้/เติม และเตือนเมื่อใกล้หมด
- 📊 **รายงานยอดขายรายวัน/รายเดือน** – ดูยอดรวม ช่องทางชำระเงิน และเมนูขายดีในรอบ 30 วัน
- 🔔 **แจ้งเตือนออเดอร์ใหม่** – ตั้งค่าได้ทั้ง Email / LINE Messaging API (เลือกได้จาก `.env`) นอกเหนือจากเสียงในหน้าครัว
- 🧑‍🍳 **หลังบ้านปรับเมนูผ่านเว็บ** – เพิ่ม/แก้ไขหมวดหมู่ รายการอาหาร และสูตรวัตถุดิบได้ด้วยฟอร์ม
- 🍲 **ตัวเลือกเส้น/ท็อปปิ้งแบบกำหนดเอง** – กำหนดกลุ่มตัวเลือก (เลือกได้ 1 หรือหลายอย่าง) พร้อมราคาพิเศษและรูปภาพเมนูจากหน้าแอดมิน

## การติดตั้งและเริ่มต้นใช้งาน
1. สร้าง Virtual Environment (แนะนำ Python 3.11 ขึ้นไป)
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate
   ```
2. ติดตั้ง dependencies
   ```bash
   pip install -r requirements.txt
   ```
3. ตั้งค่าตัวแปรสภาพแวดล้อม (กรณีต้องการกำหนดบัญชีหรือการแจ้งเตือน ดูหัวข้อถัดไป)
4. รันเซิร์ฟเวอร์
   ```bash
   flask --app app.py run --host 0.0.0.0 --port 5000
   ```
   จากนั้นเปิด `http://127.0.0.1:5000`

## บัญชีเข้าสู่ระบบ (ปรับได้ผ่าน `.env`)

| บทบาท  | ผู้ใช้ / รหัสผ่านเริ่มต้น | สิทธิ์ |
|--------|----------------------------|--------|
| ครัว   | `kitchen` / `kitchen123`   | เข้าหน้า `/kitchen` |
| ผู้ดูแล | `admin` / `admin123`       | เข้าถึง `/admin`, `/menu`, `/stock`, `/reports` และหน้าครัว |

ตัวอย่างไฟล์ `.env`
```env
KITCHEN_USERNAME=kitchen
KITCHEN_PASSWORD=kitchen123
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# การแจ้งเตือนออเดอร์ (เลือกใช้ได้)
NOTIFY_EMAIL_TO=chef@example.com,owner@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=pos@example.com
SMTP_PASSWORD=secret
SMTP_USE_TLS=true

LINE_CHANNEL_ACCESS_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxx
LINE_CHANNEL_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx
LINE_USER_IDS=Uxxxxxxxxxxxxxxx,Uyyyyyyyyyyyyyyyy
```

> หากไม่ตั้งค่า Email / LINE ระบบยังคงทำงานได้ตามปกติ เพียงแต่จะไม่ส่งแจ้งเตือนออกภายนอก และหากต้องการ broadcast ไปยังทุกเพื่อน/ห้องในช่อง ให้ปล่อย `LINE_USER_IDS` ว่างไว้

## สรุปหน้าการใช้งานหลัก
- `/` – หน้าสาธิตรวมลิงก์สำคัญ
- `/table/<table_code>` – เมนูลูกค้า (เช่น `T1`, `T2`)
- `/order/<order_id>` – หน้าติดตามสถานะออเดอร์เฉพาะ
- `/kitchen` – มุมมองครัว (ต้องล็อกอิน) มีเสียงเตือนและลิงก์ไปหน้าชำระเงิน
- `/admin` – แดชบอร์ดภาพรวม + PromptPay QR
- `/admin/orders/<id>` – รายละเอียดออเดอร์, บันทึกการชำระ, ใบเสร็จ
- `/admin/menu` – จัดการหมวดหมู่/เมนู/สูตรวัตถุดิบ
- `/admin/stock` – จัดการสต๊อก เติม/ปรับยอด และดูความเคลื่อนไหว
- `/admin/reports` – รายงานยอดขายรายวัน/เดือน, ช่องทางชำระ, เมนูขายดี
- `/admin/insights` – แดชบอร์ดสถิติเชิงลึก (เทรนด์ 7 วัน, AOV, โต๊ะ/หมวดหมู่ขายดี)
- `instant_game/` – โปรเจ็กต์ Facebook Instant Game (เกมบีบมะนาว) สำหรับแคมเปญเล่นแล้วรับส่วนลด

### Lemon Squeeze Instant Game
โฟลเดอร์ `instant_game/` มี prototype เกม HTML5 (Phaser) ที่ลูกค้าเล่นบน Facebook Instant Games ได้:
1. อัปโหลดไฟล์ในโฟลเดอร์เป็น bundle ใหม่ใน Facebook Instant Games Dashboard แล้วตั้งค่า entry เป็น `index.html`.
2. ปรับโค้ดใน `game.js` ส่วนปุ่มแชร์ (`share-btn`) ให้เรียก API `/api/game/reward` เพื่อออกคูปองส่วนลดหลังแชร์สำเร็จ
3. แจก QR/ลิงก์เกมในร้านหรือบนเพจ เพื่อให้ลูกค้าเล่นบีบมะนาว 30 วินาที แล้วแชร์ผลลัพธ์เพื่อรับส่วนลด

## การสร้าง QR Code สำหรับโต๊ะ
สคริปต์ `scripts/generate_qr_codes.py` จะสร้างไฟล์ PNG แยกตามโต๊ะ
```bash
python scripts/generate_qr_codes.py \
  --base-url http://127.0.0.1:5000 \
  --tables T1 T2 T3 \
  --output qr_codes
```
ไฟล์จะถูกบันทึกในโฟลเดอร์ `qr_codes/` (สร้างอัตโนมัติหากยังไม่มี)

### QR พร้อมเพย์สำหรับเช็คบิล
```bash
python scripts/generate_promptpay_qr.py \
  --phone 0823293693 \
  --output static/promptpay.png
```
เพิ่ม `--amount 150` หากต้องการล็อกยอดที่ลูกค้าต้องชำระ

### การบันทึกบิลหลังชำระเงิน (VAT 7%)
- เมื่อออเดอร์ถูกตั้งสถานะ `paid` ระบบจะสร้างบิลใหม่ในฐานข้อมูล `invoices` โดยคำนวณแยก **มูลค่าสินค้า**, **VAT 7%**, และ **ยอดรวม** (เช่น ยอดรวม 100 → VAT 7 บาท → มูลค่าสินค้า 93 บาท)
- บิลทุกใบสามารถดูย้อนหลัง/ดาวน์โหลดได้ที่ `/admin/invoices` หรือปุ่มทางลัดบนแดชบอร์ดผู้ดูแล
- ปุ่ม “ดาวน์โหลด” จะสร้างไฟล์ `.png` ที่มีโลโก้ร้าน รายการอาหาร และยอดตามสูตรข้างต้น พร้อมพิมพ์/ส่งต่อได้ทันที หรือกด “ส่งออก CSV” (เข้ารหัส UTF-8 พร้อม BOM รองรับ Excel ภาษาไทย) เพื่อโหลดข้อมูลทั้งหมดสำหรับนำเข้าโปรแกรมบัญชี

## โครงสร้างข้อมูลสำคัญ

| ตาราง | รายละเอียด |
|-------|------------|
| `dining_tables` | โต๊ะและรหัสสำหรับสร้าง QR |
| `menu_categories`, `menu_items` | หมวดหมู่และรายการอาหาร |
| `ingredients` | วัตถุดิบหลักในร้าน |
| `menu_item_ingredients` | สูตรปริมาณวัตถุดิบต่อหนึ่งที่ |
| `orders`, `order_items` | ออเดอร์และรายละเอียดเมนู |
| `payments` | บันทึกการชำระเงินต่อออเดอร์ |
| `stock_movements` | ประวัติการเติม/เบิก/ปรับสต๊อก |

เมื่อมีการสั่งอาหาร ระบบจะคำนวนการใช้วัตถุดิบตามสูตรและลดสต๊อกให้อัตโนมัติ หากยอดคงเหลือต่ำกว่าระดับเตือน รายการเมนูที่เกี่ยวข้องจะถูกปิดการขายจนกว่าจะเติมสต๊อก

## การแจ้งเตือนออเดอร์ใหม่ (อีเมล / LINE Messaging API)
- ตั้งค่า SMTP + อีเมลผู้รับใน `.env` เพื่อให้ระบบส่งอีเมลสรุปออเดอร์ทันทีเมื่อมีการสั่ง
- หรือใช้ LINE Messaging API โดยสร้าง Channel access token (long-lived) จาก LINE Developers เมื่อได้ `LINE_CHANNEL_ACCESS_TOKEN` แล้วให้กรอกลงใน `.env` พร้อม `LINE_USER_IDS` (User ID หรือ Group ID ที่ต้องการให้รับแจ้ง หากปล่อยว่างระบบจะ broadcast ทั้งช่อง)
- ระบบส่งใน background thread จึงไม่ทำให้การตอบ API ช้าลง

### เติมสต๊อกผ่าน LINE OA (Webhook)
พนักงานสามารถพิมพ์คำสั่งใน OA เช่น `stock แคบหมู 10` หรือ `เติม ลูกชิ้น 2 ไม้ 5` เพื่อเติมสต๊อกอัตโนมัติได้ โดยตั้งค่าเพิ่มเติมดังนี้

1. ใน LINE Developers ให้สร้าง/เลือก Messaging API Channel ของ OA ที่ต้องการ
2. ไปที่แท็บ **Messaging API** แล้ว
   - เปิด `Use webhook` เป็น ON
   - กรอก `Webhook URL` เป็น `https://<โดเมนของคุณ>/line/webhook`
   - กด **Verify** เพื่อทดสอบว่าระบบตอบสถานะ 200 ได้
3. ในแท็บ **Basic settings** กด `Issue` เพื่อลองรับ **Channel secret** แล้วบันทึกไว้
4. ในแท็บ **Messaging API** เลื่อนไปที่หัวข้อ Channel access token แล้วกด `Issue` สำหรับ **long-lived token**
5. กรอกข้อมูลใน `.env`
   ```env
   LINE_CHANNEL_ACCESS_TOKEN=<long-lived token>
   LINE_CHANNEL_SECRET=<channel secret>
   ```
6. Restart แอปเพื่อให้โหลดค่าใหม่ แล้วทดสอบพิมพ์ใน OA:
   - พิมพ์ `stock <ชื่อวัตถุดิบ> <จำนวน>` หรือ `เติม <ชื่อวัตถุดิบ> <จำนวน>`
   - ระบบจะตอบกลับในห้อง LINE และบันทึกสต๊อก + สร้างบันทึก `StockMovement` ให้อัตโนมัติ

> หมายเหตุ: ชื่อวัตถุดิบต้องตรงกับที่ตั้งไว้ในระบบ และจำนวนต้องเป็นตัวเลขมากกว่า 0 (รองรับทศนิยม เช่น 2.5) หากไม่พบวัตถุดิบหรือกรอกไม่ถูกต้อง ระบบจะแจ้งเตือนกลับผ่าน LINE ทันที

## Roadmap ที่อาจเพิ่มได้ในอนาคต
1. ระบบสมาชิก/คะแนนสะสม และโปรโมชันแบบตั้งเวลาล่วงหน้า
2. รองรับการพิมพ์สลิปครัว/เครื่องพิมพ์ใบเสร็จอัตโนมัติ
3. Multi-branch / Multi-tenant (มากกว่าหนึ่งสาขาในฐานข้อมูลเดียว)
4. วิเคราะห์ยอดขายเชิงลึก (เช่น ช่วงเวลาพีค, ยอดขายตามพนักงาน)

โค้ดระบบหลักอยู่ในโฟลเดอร์ `pos_app/` สามารถต่อยอดด้วย Blueprint หรือ Service เพิ่มเติมได้ทันที หากแก้ไขโครงสร้างฐานข้อมูลให้ปรับ `models.py` และเรียก `seed_sample_data(force=True)` เพื่อรีเซ็ตข้อมูลตัวอย่าง
# pos-ninekerl
